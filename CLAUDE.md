# 状態遷移図構築支援アプリケーション

## 概要
このWebアプリケーションは、直感的な操作で状態遷移図を作成できる教育支援ツールです。PCとスマートフォンの両方に対応しており、ドラッグ&ドロップやタッチ操作で状態遷移図を構築できます。コンピュータシステムの設計における状態の変化とその条件を視覚的に表現し、教育現場から実務まで幅広く活用できます。

## 機能

### 基本機能
- **状態図形の配置**: 初期状態、通常状態、終了状態の図形を配置
- **遷移線の描画**: 状態間の遷移を矢印で表現
- **遷移条件の設定**: 遷移線上に条件やイベントを記述
- **状態の命名**: 状態図形内にテキストを直接編集
- **図形の移動**: ドラッグ&ドロップによる状態図形の自由な移動
- **削除機能**: 状態や遷移線の削除

### データ管理機能
- **JSONファイル保存・読み込み**: 
  - 作成した状態遷移図をJSONファイルとして保存
  - 保存されたJSONファイルから状態遷移図を復元
  - プロジェクトファイルの管理とバージョン管理対応
- **テンプレート機能**:
  - よく使用される状態遷移パターンのテンプレート
    - 目覚まし時計パターン（アラーム、スヌーズ機能）
    - 自動販売機パターン（待機、選択、決済）
    - ログインシステムパターン（認証、セッション管理）
    - 通信プロトコルパターン（接続、データ転送、切断）
    - ゲーム状態パターン（メニュー、プレイ、ポーズ、ゲームオーバー）
  - カスタムテンプレートの作成・保存機能

### 共有・エクスポート機能
- **URLでの共有**: 
  - 状態遷移図データをURL化して共有
  - 閲覧専用リンクと編集可能リンクの選択
  - QRコード生成による簡単共有
- **画像形式エクスポート**: 
  - PNG/JPEG形式での高品質画像出力
  - 解像度とサイズの指定可能
  - 背景色の選択（透明・白・カスタム）
- **その他のエクスポート**: 
  - SVG形式での出力
  - 印刷最適化されたレイアウト
  - 状態遷移表（テーブル形式）への出力

### 操作性向上機能
- **アンドゥ・リドゥ機能**: 
  - 無制限の操作履歴管理
  - ショートカットキー対応（Ctrl+Z / Ctrl+Y）
  - 操作履歴の可視化
- **グリッドスナップ機能**: 
  - 状態図形配置時の自動グリッド吸着
  - グリッドサイズの調整可能
  - グリッド表示のオン/オフ切り替え
- **ズーム・パン機能**: 
  - マウスホイール・ピンチジェスチャーによるズーム
  - 自由なパン（移動）操作
  - フィット表示・実際のサイズ表示
  - ミニマップ表示による全体把握
- **図形の整列・配置機能**: 
  - 複数状態の整列（左揃え、中央揃え、右揃え等）
  - 等間隔配置機能
  - 状態図形の重なり順序の調整
  - 自動レイアウト機能（階層配置、円形配置）

### 検証・解析機能
- **状態遷移図の論理チェック**: 
  - 状態遷移の整合性チェック
  - 初期状態の存在確認
  - 終了状態への到達可能性チェック
- **到達不可能な状態の検出**: 
  - デッドステート（到達不可能な状態）の自動検出
  - 孤立した状態の警告
  - 状態遷移経路の可視化
- **無限ループの警告**: 
  - 循環遷移の検出
  - 終了条件がない状態遷移の警告
  - 状態遷移の最適化提案
- **状態遷移表の生成**: 
  - 現在状態×イベント→次状態の表形式出力
  - 遷移条件の一覧表示
  - 欠損遷移の検出

### 対応デバイス
- **PC**: マウス操作によるドラッグ&ドロップ、キーボードショートカット
- **スマートフォン/タブレット**: タッチ操作対応、ジェスチャー操作
- **レスポンシブデザイン**: 画面サイズに応じた最適なレイアウト

### エラーハンドリング
- 無効な遷移の防止
- 状態の重複チェック
- 操作エラーの適切な通知
- 自動保存機能によるデータ保護

## 使用方法

### 状態の配置
1. 左側のツールパネルから使用したい状態種類を選択
   - 初期状態（黒塗りの円）
   - 通常状態（円または角丸四角形）
   - 終了状態（二重円）
2. キャンバス上の配置したい位置をクリック
3. グリッドスナップが有効な場合、自動的に格子点に配置
4. 状態をダブルクリックして名前を編集

### 遷移線の描画
1. 遷移元の状態をクリックして選択
2. 「遷移モード」をオンにする
3. 遷移先の状態をクリック
4. 自動的に最適な接続点を選択し、矢印を描画
5. 遷移線をダブルクリックして条件を編集

### 状態の移動・整列
- **移動**: PC（ドラッグ&ドロップ）、スマートフォン（タッチして移動）
- **整列**: 複数状態を選択後、整列ボタンをクリック
- **グリッドスナップ**: 有効にすると移動時に自動的にグリッドに吸着

### ズーム・パン操作
- **ズーム**: マウスホイール、ピンチジェスチャー、ズームボタン
- **パン**: 中ボタンドラッグ、二本指ドラッグ、パンモード
- **フィット**: 全体表示ボタンで状態遷移図全体を画面に収める

### 保存・読み込み
1. **保存**: ファイルメニューから「JSONで保存」を選択
2. **読み込み**: ファイルメニューから「JSONを開く」を選択
3. **テンプレート**: テンプレートライブラリから選択して適用

### 共有・エクスポート
1. **URL共有**: 共有ボタンから共有リンクを生成
2. **画像エクスポート**: エクスポートメニューから形式を選択
3. **状態遷移表**: 表形式でのエクスポートも可能

### 検証機能の使用
1. **論理チェック**: 検証メニューから「状態遷移検証」を実行
2. **警告確認**: 検出された問題点をリストで確認
3. **修正**: 問題箇所をクリックして該当状態にジャンプ

### 削除
- 状態または遷移線を選択してDeleteキーを押す
- 右クリックメニューから削除を選択
- 複数選択による一括削除

## 技術仕様

### 使用技術
- HTML5
- CSS3 (レスポンシブデザイン、CSS Grid、Flexbox)
- JavaScript (ES6+)
- Canvas API / SVG
- Web APIs (File API, URL API, Clipboard API)

### 対応ブラウザ
- Chrome (推奨)
- Firefox
- Safari
- Edge

### パフォーマンス要件
- 500個以上の状態でもスムーズな操作
- リアルタイムでの検証処理
- 大容量ファイルの高速読み込み

### ファイル構成
```
/
├── index.html              # メインHTMLファイル
├── css/
│   ├── style.css          # メインスタイルシート
│   ├── responsive.css     # レスポンシブデザイン
│   └── themes.css         # テーマカラー設定
├── js/
│   ├── app.js             # メインアプリケーションロジック
│   ├── statediagram.js    # 状態遷移図描画エンジン
│   ├── validation.js      # 検証エンジン
│   ├── export.js          # エクスポート機能
│   ├── templates.js       # テンプレート管理
│   ├── statetable.js      # 状態遷移表生成
│   └── utils.js           # ユーティリティ関数
├── templates/
│   ├── basic-patterns.json    # 基本パターンテンプレート
│   ├── system-patterns.json  # システム系パターンテンプレート
│   ├── game-patterns.json    # ゲーム系パターンテンプレート
│   └── custom/               # カスタムテンプレート
├── assets/
│   ├── icons/             # アイコンファイル
│   └── images/            # 画像ファイル
└── README.md              # このファイル
```

## インストール・実行

### GitHub Pagesでの実行
1. GitHubリポジトリをクローン
2. GitHub Pagesを有効化
3. ブラウザでアクセス

### ローカル実行
1. ファイルをローカルにダウンロード
2. `index.html`をブラウザで開く
3. 一部機能（ファイル保存等）にはHTTPサーバーが必要

### 開発環境
```bash
# 開発サーバーの起動（推奨）
npx http-server .
# または
python -m http.server 8000
```

## ショートカットキー

### 基本操作
- `Ctrl + Z`: アンドゥ
- `Ctrl + Y`: リドゥ
- `Ctrl + S`: 保存
- `Ctrl + O`: 開く
- `Delete`: 選択項目の削除
- `Ctrl + A`: 全選択

### 表示操作
- `Ctrl + +`: ズームイン
- `Ctrl + -`: ズームアウト
- `Ctrl + 0`: 100%表示
- `F`: フィット表示
- `G`: グリッド表示切り替え

### 状態操作
- `Ctrl + D`: 複製
- `Ctrl + G`: グループ化
- `Ctrl + Shift + G`: グループ解除
- `Arrow Keys`: 選択状態の微調整
- `T`: 遷移モード切り替え

### 専用機能
- `V`: 検証実行
- `Ctrl + E`: エクスポートメニュー
- `Ctrl + T`: テンプレート選択

## 教育機能

### 学習支援機能
- **インタラクティブチュートリアル**: 
  - 目覚まし時計の状態遷移図作成ガイド
  - 自動販売機の状態遷移図作成ガイド
- **演習問題**: 
  - 基本的な状態遷移図の作成課題
  - 問題文から状態遷移図への変換練習
- **正解例との比較**: 
  - 作成した図形と正解例の比較機能
  - 差分の可視化

### 問題集連携機能
- **問題文の表示**: 
  - 右パネルに問題文を表示
  - 状態遷移図作成の条件を明確化
- **空欄補完モード**: 
  - 部分的に完成した状態遷移図に遷移条件を入力
  - 選択肢からの選択機能
- **採点機能**: 
  - 作成した状態遷移図の自動採点
  - 正解率と改善点の表示

## 制作者
**Created by Dit-Lab.(Daiki ITO)**  
**Supported by Tomoaki ATSUMI**

## ライセンス
このプロジェクトは教育目的で作成されています。
